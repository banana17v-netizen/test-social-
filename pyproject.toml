[project]
name = "social-listening-harness"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pydantic>=2", "langgraph", "google-genai", "streamlit"]

[tool.pytest.ini_options]
pythonpath = ["."]

